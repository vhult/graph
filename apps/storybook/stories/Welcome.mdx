import { Meta, Story } from "@storybook/addon-docs/blocks";
import * as SmallGraph from "./showcase/SmallGraph.stories";

<Meta title="Welcome" />

# @vhult/graph

A WebGPU graph renderer. It draws millions of nodes and edges from a worker, and does nothing else: no layout, no analysis.

Drag a node, hover it, scroll to zoom.

<Story of={SmallGraph.SmallGraph} />

## Sections

- **Showcase**: purpose-built graphs, some computed on the GPU every frame.
- **Large graph**: one graph, five layouts, from 100 to 10 million nodes.
- **Nodes** and **Edges**: what each can look like.
- **Stress**: the memory ceiling.
- **Developer**: the sandbox with every option, benchmarks and correctness checks.
